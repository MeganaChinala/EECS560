Input or data text format:

restaurantname (with no spaces), Rating (1-5), price ($, $$, $$$, $$$$)

example:

mcdonalds, 2, $
tacobell, 3, $
starbucks, 5, $$$

To run:
run make
execute:
  ./DoubleHashing textFile.txt
